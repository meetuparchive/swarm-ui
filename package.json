{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "lerna version --conventional-commits --yes && lerna publish --from-package"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  }
}
